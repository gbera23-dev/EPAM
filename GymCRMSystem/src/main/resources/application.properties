
# Data to be injected to database(database.initialize must be toggled to inject data into database)
database.initialize=false

data.TraineeDataPath=TraineeData.json

data.TrainerDataPath=TrainerData.json

data.TrainingDataPath=TrainingData.json

# Database credentials

db.url=jdbc:mysql://localhost:3306/Gym_CRM_db
db.username=giga
db.password=
db.driver =com.mysql.cj.jdbc.Driver

# JPA properties
jpa.packages_to_scan=entities
hibernate.show_sql=true
hibernate.hbm2ddl.auto=update
